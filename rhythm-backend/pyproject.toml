[project]
name = "rhythm-backend"
version = "0.1.0"
description = "FastAPI backend for Rhythm voice parsing and LLM calls."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.111,<1",
    "httpx>=0.27,<1",
    "pydantic>=2.7,<3",
    "uvicorn[standard]>=0.30,<1",
]

[tool.uv]
package = false
